import { eventSubscription } from '../../utils/eventSubscription';

export const orderPlannerEvents = eventSubscription();
/** Changes that affect the Smart Order list or its calculation. */
export const smartOrderEvents = eventSubscription();
