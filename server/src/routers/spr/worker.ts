// Skeleton for SPR worker (no implementation)
// Key imports to uncomment during implementation (mirrors guild/worker.ts):
// import { work } from '../../utils/workerBase';
// import { sprUnifier } from './sprUnifier';

// TODO list:
// - Use work({ process: async ({ progress, utils: { customMessage } }) => { await sprUnifier.updateUnifiedTable({ progress, onUpdateCallback: (uniId) => customMessage(uniId.toString()) }); } })
// - Export nothing; worker is referenced via managedWorker in index.ts
