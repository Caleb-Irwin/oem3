{
  "name": "oem3",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.4.2"
  },
  "scripts": {
    "dev": "cd server && bun run dev",
    "build": "docker build .",
    "start": "docker compose up",
    "preview": "docker compose up --build"
  },
  "type": "module",
  "dependencies": {}
}
