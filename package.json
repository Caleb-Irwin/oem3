{
  "name": "oem3",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "cd server && bun run dev",
    "build": "apt-get install lib32stdc++6 && cd server && bun run build",
    "start": "cd server && bun run start",
    "preview": "cd server && bun run preview"
  },
  "type": "module",
  "dependencies": {
    "svelte-intersection-observer": "^1.0.0"
  }
}