{
  "name": "oem3",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "cd server && bun run dev",
    "build": "docker build .",
    "start": "docker compose up",
    "preview": "docker compose up --build"
  },
  "type": "module",
  "dependencies": {}
}